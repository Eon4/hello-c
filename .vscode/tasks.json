{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Build C code",
        "type": "shell",
        "command": "gcc",
        "args": [
          "${file}",
          "-o",
          "${fileDirname}/${fileBasenameNoExtension}"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": [],
        "options": {
          "cwd": "${fileDirname}"
        },
        "presentation": {
          "reveal": "silent"
        }
      }
    ]
  }
  